apiVersion: v1
kind: Service
metadata:
  name: aidlc-maintenance-service
  namespace: aidlc-demo
  labels:
    app: aidlc-maintenance
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: aidlc-maintenance
